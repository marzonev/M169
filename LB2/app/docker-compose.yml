version: '3.8'

services:
  php:
    build: ./backend/php
    container_name: m169-php
    volumes:
      - ./backend/php:/var/www/html

  nginx:
    build: ./nginx
    container_name: m169-nginx
    ports:
      - "80:80"
    volumes:
      - ./backend/php:/var/www/html
    depends_on:
      - php
